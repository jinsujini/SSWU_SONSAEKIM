<%- include('../layouts/header') %>
<link rel="stylesheet" href="/css/auth/verify.css">

<div class="email-auth-page">
  <div class="login-page">
    <div class="login-box">
      <form action="/auth/verify" method="POST" class="email-auth-form">
        <h2>회원가입 이메일 인증</h2>
        <p>
          <%= email %>로<br />
          회원가입 인증번호를 전달하였습니다.<br />
          아래에 인증번호를 입력해주세요.
        </p>

        <input type="hidden" name="email" value="<%= email %>"/>

        <input 
          type="text" 
          name="code" 
          class="code-input"
          value="<%= typeof code !== 'undefined' ? code : '' %>"
        />

        <% if (typeof errorMessage !== 'undefined') { %>
          <span class="error-message"><%= errorMessage %></span>
        <% } %>

        <button type="submit" class="login-btn">확인</button>
      </form>
    </div>

    <div class="find-password">
      <a href="/auth/register">회원정보 다시 입력</a>
    </div>
  </div>
</div>
<%- include('../layouts/footer') %>